# Icons

This directory contains icon files.

## Guidelines

- Preferably use SVG format for scalability
- Use consistent sizing where possible
- Use descriptive filenames (e.g., `user-icon.svg`, `settings-icon.svg`)
